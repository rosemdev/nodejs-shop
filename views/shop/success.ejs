<%- include('../includes/head.ejs') %>
</head>

<body>
    <%- include('../includes/navigation.ejs') %>
    <div class="centered">
        <div>
            <h1>The payment was succsessful</h1>
            <h2>Thank you for placing the order</h2>
        </div>
        <hr>
        <div>
            <h3>Your order id <%= order._id.toString() %></h3>
            <ul>
                <% for (let lineitem of order.products) { %>
                    <li><%= lineitem.product.title %> x <%= lineitem.quantity %> - $<%= lineitem.product.price %></li>
                <% } %>
                <h2>Total: <%= order.total %></h2>
            </ul>
        </div>

        <a class="btn" href="/orders">See all orders</a>
    </div>

<%- include('../includes/end.ejs') %>